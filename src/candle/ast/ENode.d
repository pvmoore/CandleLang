module candle.ast.ENode;

import candle.all;

enum ENode {
    PROJECT, UNIT, NODE_REF,

    AS, BINARY, BUILTIN_FUNC, CALL, CHAR, DOT, ID, IS,
    NULL, NUMBER, PARENS, PROJECT_ID, SCOPE, STRING, UNARY,

    VAR, RETURN,

    ARRAY, ENUM, FUNC, POINTER, PRIMITIVE, STRUCT, TYPE_REF, UNION
}