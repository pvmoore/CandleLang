/*
    STRUCT ::= 'struct' name '{' { Var } '}'
*/

func struct_() {
    local:Local;
    local2:Local = {};

    // @sizeOf(S)
    // @offsetOf(S, field)
    // @alignOf(S, field)
}

// Must have at least 1 member
//struct Empty {}

struct Local {
    a:byte;
    b:float;
    inner:Inner;
}

struct Inner {
    a:long;
    b:long;
}
